# Graph Index (generated)

`graph.db` dibangun dari `../nodes/**/*.md` + `../edges/edges.jsonl` saat
build time (SQLite atau graph-lib — pilihan implementasi belum dikunci,
lihat §16 butir 2). File ini **tidak di-commit**; lihat `.gitignore` di
root repo.

Build script belum ada di v0.1 (Volume 1 adalah blueprint, bukan
implementasi) — akan ditulis di Volume 2 bersamaan dengan seeding graph.
